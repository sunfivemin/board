<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>프로필</title>
    <link rel="stylesheet" href="/css/main.css" />
    <script src="/js/nav.js" defer></script>
  </head>
  <body>
    <%- include('../partials/nav') %>

    <div class="container">
      <div class="profile-container">
        <h1>프로필</h1>
        <% if (user) { %>
        <div class="profile-info">
          <div class="profile-info-item">
            <span class="profile-info-label">사용자명</span>
            <span class="profile-info-value"><%= user.username %></span>
          </div>
          <div class="profile-info-item">
            <span class="profile-info-label">이메일</span>
            <span class="profile-info-value"><%= user.email %></span>
          </div>
          <div class="profile-info-item">
            <span class="profile-info-label">가입일</span>
            <span class="profile-info-value">
              <%= new Date(user.createdAt).toLocaleDateString('ko-KR', { year:
              'numeric', month: 'long', day: 'numeric' }) %>
            </span>
          </div>
        </div>
        <% } else { %>
        <p>사용자 정보를 찾을 수 없습니다.</p>
        <% } %>
      </div>
    </div>
  </body>
</html>
